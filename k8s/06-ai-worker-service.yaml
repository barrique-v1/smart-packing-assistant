apiVersion: v1
kind: Service
metadata:
  name: ai-worker
  namespace: packing-assistant
  labels:
    app: ai-worker
    component: ai-service
spec:
  type: ClusterIP
  selector:
    app: ai-worker
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
